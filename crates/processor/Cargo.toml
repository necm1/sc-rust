[package]
name = "processor"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
buffer-reader = { path = "../buffer-reader" }
shared = { path = "../shared" }
async-trait = "0.1"
